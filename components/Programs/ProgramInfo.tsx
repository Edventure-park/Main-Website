/* eslint-disable no-unused-vars */
import React from 'react'

interface ProgramInfoProps {
    Heading: string;
    SubHeading: string;
    Paragraph: string;
    lists: string[];
}

function ProgramInfo({ProgramInfoData}:{ProgramInfoData:ProgramInfoProps}) {
  return (
    <div>
      hmmm
    </div>
  )
}

export default ProgramInfo
